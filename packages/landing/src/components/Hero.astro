<style>
  .logo {
    width: 180px;
    height: auto;
    margin-bottom: 3rem;
    object-fit: contain;
  }

  h1 {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--text);
    letter-spacing: -0.025em;
  }

  .subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    margin-bottom: 2.5rem;
    max-width: 500px;
    line-height: 1.7;
  }

  .cta-group {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin-bottom: 3rem;
  }

  .btn-primary {
    background-color: var(--primary);
    color: var(--white);
    padding: 0.875rem 2rem;
    border-radius: 0.75rem;
    font-weight: 600;
    font-size: 1.1rem;
    text-decoration: none;
    transition: all 0.2s;
    box-shadow: 0 4px 12px rgba(51, 24, 117, 0.25);
    display: inline-block;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(51, 24, 117, 0.35);
  }

  @media (max-width: 1024px) {
    h1 { font-size: 2.5rem; }
    .subtitle { font-size: 1.05rem; }
  }

  @media (max-width: 600px) {
    h1 { font-size: 2rem; }
  }
</style>

<img src="/logo-trimmed.png" alt="Orbital Logo" class="logo" />
<h1>Stay in orbit with your network.</h1>
<p class="subtitle">
  A Personal Relationship Manager that helps you maintain your professional and
  personal connections — without the stress.
</p>

<div class="cta-group">
  <a href="#" class="btn-primary">Get Started</a>
</div>
